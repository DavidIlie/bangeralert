{
   "pipeline": {
      "build": {
         "dependsOn": [
            "^build",
            "$DATABASE_URL",
            "$AUTH_SPOTIFY_CLIENT_ID",
            "$AUTH_SPOTIFY_CLIENT_SECRET",
            "$NEXTAUTH_URL",
            "$NEXTAUTH_SECRET",
            "$NEXTAUTH_EXPO_URL"
         ],
         "outputs": [".next/**", ".expo/**"]
      },
      "lint": {
         "outputs": []
      },
      "dev": {
         "cache": false
      },
      "postinstall": {}
   }
}
